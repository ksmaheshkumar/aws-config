# This is the file that sourced when you log in as a jenkins user.
# For the file that is sourced by jenkins build jobs, check out .bashrc.
export PATH="/usr/local/google_appengine:/home/ubuntu/google-cloud-sdk/bin:$HOME/git-bigfile/bin:$PATH"
