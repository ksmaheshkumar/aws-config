# This allows you to install postfix without it prompting you about
# anything.  To use this you need to do
#   sudo apt-get install debconf-utils
# and then 
#   sudo debconf-set-selections postfix.preseed

postfix postfix/mailname        string  domain-redirect.khanacademy.org
postfix postfix/main_mailer_type	select	Internet Site
